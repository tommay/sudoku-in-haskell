module net.tommay.spudoku.Stats where

abstract data Stats = Stats {
  guesses :: Int
}

new :: Stats
new =
  Stats.Stats {
    guesses = 0
  }

guess :: Stats -> Stats
guess this =
  this.{ guesses = Stats.guesses this + 1 }
