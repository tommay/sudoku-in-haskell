module net.tommay.spudoku.Placed where

import net.tommay.spudoku.Digit (Digit)

abstract data Placed = Placed {
  cellNumber :: Int,
  digit :: Digit
}

new :: Int -> Digit -> Placed
new cellNumber digit =
  Placed.Placed {
    cellNumber = cellNumber,
    digit = digit
  }
